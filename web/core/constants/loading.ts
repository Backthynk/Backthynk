/**
 * Constants for the loading animation behavior
 * @deprecated Use `loading` from @core/config instead
 */

import { loading } from '../config';

/**
 * Minimum time to show the loading animation (in milliseconds)
 * @deprecated Use `loading.minTime` from @core/config instead
 */
export const MIN_LOADING_TIME = loading.minTime;

/**
 * Maximum time to wait before showing the app (in milliseconds)
 * @deprecated Use `loading.maxTime` from @core/config instead
 */
export const MAX_LOADING_TIME = loading.maxTime;
